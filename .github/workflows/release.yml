name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: diaryx-org/.github/.github/workflows/reusable-plugin-release.yml@main
    with:
      plugin-id: diaryx.storage-s3
      crate-name: diaryx_storage_s3_extism
    secrets:
      REGISTRY_PAT: ${{ secrets.REGISTRY_PAT }}
